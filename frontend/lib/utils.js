import {getSupportIndex, getDocsSidebar} from "api"

const file_name_based_data_fetching = async () => {
    const current_file_name = __filename.slice(__dirname.length + 1, -3)

}

export default file_name_based_data_fetching;